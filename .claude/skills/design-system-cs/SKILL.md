---
name: junseok-cs-study-design
description: Use this skill to generate well-branded interfaces and assets for JUNSEOK CS study (비전공자를 위한 10주 CS 집중 커리큘럼 웹사이트), either for production or throwaway prototypes/mocks. Contains essential design guidelines, colors, type, fonts, assets, and UI kit components for prototyping.
user-invocable: true
---

Read the `README.md` file within this skill, and explore the other available files — `colors_and_type.css`, `tokens.css`, `assets/`, `preview/`, and the two UI kits in `ui_kits/` (`course_list` and `course_detail`).

If creating visual artifacts (slides, mocks, throwaway prototypes), copy assets out and create static HTML files for the user to view. Always link `colors_and_type.css` and `tokens.css`, and reuse the existing JSX components from the UI kits where possible.

If working on production code, copy the assets and read the rules in `README.md` to become an expert in designing with this brand — especially the CONTENT FUNDAMENTALS (해요체, no emoji, 한글+영문 spacing rules) and VISUAL FOUNDATIONS (indigo primary, 6 stage colors, soft multi-layer shadows, no bluish-purple gradient slop) sections.

If the user invokes this skill without any other guidance, ask them what they want to build or design, ask some clarifying questions (target screen, which stage, dark/light mode, etc.), and act as an expert designer who outputs HTML artifacts or production code depending on the need.
